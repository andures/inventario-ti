{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && NODE_OPTIONS='--max-old-space-size=4096' npm run build"
  },
  "deploy": {
    "startCommand": "npx serve@14.2.5 dist -l $PORT"
  }
}
